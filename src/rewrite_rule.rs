// This is the main system that allows our CAS to simplify algebra systems

Trait operator {
    fn same_operator(&self, )
}

type RewriteRule = (Operator, Operator);
type RewriteSystem = Vec<RewriteRule>;

bool matchPatern()
